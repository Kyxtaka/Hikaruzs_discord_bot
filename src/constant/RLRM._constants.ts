const RLRM_GUILDS_ID: Array<string> = []; //every guilds ID that active the random link to random member service
const interval10seconds = 1000 * 10; // 10 seconds 
const interval24 = 1000 * 60 * 60 * 24; // 24 hours
const RLRM_interval = interval24;

export {
    RLRM_GUILDS_ID, 
    interval10seconds, 
    interval24,
    RLRM_interval
};